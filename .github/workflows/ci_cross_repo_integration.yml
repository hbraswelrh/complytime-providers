# Cross-repo integration test workflow.
# Builds providers (ampel + opa) from the current PR/push, checks out
# complyctl@main, and runs the cross-repo integration test script
# to validate the full provider + complyctl pipeline end-to-end.
name: Cross-Repo Integration Test

on:
    push:
        branches:
            - main
    pull_request:

permissions:
    contents: read

jobs:
    cross-repo-integration:
        name: Cross-Repo Integration Test
        runs-on: ubuntu-latest

        steps:
            - name: Checkout complytime-providers
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  persist-credentials: false

            # Intentionally tracks main for HEAD-to-HEAD cross-repo integration.
            # This is a same-org repo under the same maintainer trust boundary.
            - name: Checkout complyctl
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  repository: complytime/complyctl
                  ref: main
                  path: _complyctl
                  persist-credentials: false

            # Use the higher Go version from the two repos so both can build
            # regardless of which side bumps first.
            - name: Determine Go version
              id: go-version
              run: |
                  v1=$(awk '/^go / {print $2}' go.mod)
                  v2=$(awk '/^go / {print $2}' _complyctl/go.mod)
                  max=$(printf '%s\n%s\n' "$v1" "$v2" | sort -V | tail -1)
                  echo "version=$max" >> "$GITHUB_OUTPUT"

            - name: Set up Go
              uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
              with:
                  go-version: ${{ steps.go-version.outputs.version }}
                  cache: false

            - name: Build providers
              run: make build

            - name: Build complyctl
              run: make build
              working-directory: _complyctl

            # Workaround: v1.2.3 writes unexpanded $HOME to GITHUB_PATH
            # when using the default install-dir. Use an absolute path
            # until upstream fix lands (carabiner-dev/actions).
            - name: Install snappy
              uses: carabiner-dev/actions/install/snappy@d41fe10fe88deaf493ce026007da73738b9f570c # v1.3.0
              with:
                  install-dir: /home/runner/.carabiner

            - name: Install ampel
              uses: carabiner-dev/actions/install/ampel@d41fe10fe88deaf493ce026007da73738b9f570c # v1.3.0
              with:
                  install-dir: /home/runner/.carabiner

            - name: Install conftest
              run: go install github.com/open-policy-agent/conftest@v0.68.2

            - name: Run cross-repo integration test
              env:
                  PROVIDERS_BIN_DIR: ${{ github.workspace }}/bin
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: make test-cross-repo COMPLYCTL_DIR=_complyctl
